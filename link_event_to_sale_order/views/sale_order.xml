<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<record id="sale_order_with_event_form_view" model="ir.ui.view">
		<field name="name">sale.order.form.with.event</field>
		<field name="model">sale.order</field>
		<field name="inherit_id" ref="sale.view_order_form"/>
		<field name="priority" eval="17"/>
		<field name="arch" type="xml">
			<xpath expr="//field[@name='state']" position="before" >
				<button name="action_cancel_with_event"
						id="action_cancel_with_event"
						states="draft,sent,sale"
						type="object"
						string="Cancel (with event)"/>
				<button name="action_draft_with_event"
						states="cancel"
						type="object"
						string="Set to Quotation (and Event to draft)"/>
			</xpath>
		</field>
	</record>
</odoo>